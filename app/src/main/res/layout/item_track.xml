<?xml version="1.0" encoding="utf-8"?>
<!-- Ported from Auxio's item_song. The drag handle is this app's own addition, taking the menu
     button's place while a playlist is being reordered so the row keeps one trailing control. -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/track_frame"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="@dimen/spacing_medium"
    android:paddingTop="@dimen/spacing_mid_medium"
    android:paddingEnd="@dimen/spacing_mid_medium"
    android:paddingBottom="@dimen/spacing_mid_medium">

    <org.fossify.musicplayer.views.CoverView
        android:id="@+id/track_image"
        android:layout_width="@dimen/size_touchable_small"
        android:layout_height="@dimen/size_touchable_small"
        android:importantForAccessibility="no"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <org.fossify.commons.views.MyTextView
        android:id="@+id/track_title"
        style="@style/Widget.TrackItem.Primary"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/spacing_mid_medium"
        app:layout_constraintBottom_toTopOf="@+id/track_info"
        app:layout_constraintEnd_toStartOf="@+id/track_menu"
        app:layout_constraintStart_toEndOf="@+id/track_image"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        tools:text="Track title" />

    <org.fossify.commons.views.MyTextView
        android:id="@+id/track_info"
        style="@style/Widget.TrackItem.Secondary"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/spacing_mid_medium"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/track_menu"
        app:layout_constraintStart_toEndOf="@+id/track_image"
        app:layout_constraintTop_toBottomOf="@+id/track_title"
        tools:text="Track artist" />

    <org.fossify.musicplayer.views.RippleFixMaterialButton
        android:id="@+id/track_menu"
        style="@style/Widget.TrackItem.Menu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:icon="@drawable/ic_more_24"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/track_drag_handle"
        android:layout_width="@dimen/size_touchable_small"
        android:layout_height="@dimen/size_touchable_small"
        android:padding="@dimen/spacing_small"
        android:src="@drawable/ic_drag_handle_vector"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
